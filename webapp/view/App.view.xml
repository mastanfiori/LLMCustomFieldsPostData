<mvc:View
    controllerName="com.nttdata.lakeshore.r300poconfirmation.controller.App"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
        <App
            id="app"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
</mvc:View>